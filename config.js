/**
 * Firebase Configuration Placeholder
 * * IMPORTANT: This file defines global variables that are automatically injected 
 * by the execution environment (Canvas) for secure and persistent data storage 
 * using Firestore.
 * * The variables defined here (__app_id, __firebase_config, __initial_auth_token)
 * MUST be present for the HTML/JavaScript application to initialize Firebase 
 * and enable user-specific progress saving and loading.
 * * You do not need to edit this file.
 */

// Placeholder for the unique application ID
window.__app_id = "canvas-spiritual-gifts-assessment-v1"; 

// Placeholder for the Firebase configuration object (needs to be a string here)
window.__firebase_config = JSON.stringify({
    apiKey: "FIREBASE_API_KEY_PLACEHOLDER",
    authDomain: "PROJECT_ID.firebaseapp.com",
    projectId: "PROJECT_ID",
    storageBucket: "PROJECT_ID.appspot.com",
    messagingSenderId: "SENDER_ID",
    appId: "APP_ID"
});

// Placeholder for the custom authentication token
window.__initial_auth_token = "FIREBASE_CUSTOM_AUTH_TOKEN_PLACEHOLDER";
